"""
Shared framework layer (task-agnostic).

Task-specific clinical logic lives under ``src.tasks.<task_name>``.
Delirium legacy pipeline modules remain at ``src.pipeline``, ``src.agents``, etc.
until fully migrated — see ``src.tasks.delirium``.
"""
